{
  "name": "scss",
  "version": "1.0.0",
  "description": "``` npm install -g scss-compile ``` ###Usage ``` npm run scss-compile ```",
  "main": "index.html",
  "scripts": {
    "watch": "sass --watch scss:scss",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "scss-compile": "^0.1.7"
  }
}
